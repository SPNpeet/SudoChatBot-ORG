# กติกาการทำงานในโปรเจกต์นี้

ไฟล์นี้ถูกโหลดอัตโนมัติทุกครั้งที่เปิด session ใหม่ — ไม่ต้องมีใครจำไปวางเอง
สั้นโดยเจตนา กติกาที่ยาวเกินจะถูกข้าม

## ระบบนี้คืออะไร (ณ 30 ก.ค. 2569)

**ระบบบัญชีออนไลน์ + ผู้ช่วยบัญชี AI สำหรับ SME ไทย** — ออกเอกสารขาย · อ่านบิลด้วย AI ·
ลงสมุดรายวันอัตโนมัติ · รายงานภาษี (ภ.พ.30 · ภ.ง.ด.3/53 · 50 ทวิ) · ไฟล์ยื่นสรรพากร

**มีลูกค้าจ่ายเงินใช้จริงอยู่บน production ตอนนี้** ทุกการแก้คือการแก้ของที่มีคนใช้อยู่

Next.js 15 + Supabase (`uafnpbawajgonarvlurj`) + Vercel · โค้ดจริงอยู่โฟลเดอร์นี้เท่านั้น
`MASTER-PROMPT.md` เป็นของยุคแชทบอทขายของก่อน pivot — **ล้าสมัย อย่าใช้อ้างอิง**
สถานะงานล่าสุดและงานค้างอยู่ใน `HANDOFF.md`

---

## 1. ขอบเขต — สาเหตุอันดับหนึ่งที่ทำระบบพัง

เจ้าของสรุปปัญหาไว้ว่า *"ทำเกิน ขาด หรือตัดทิ้ง รวมถึงข้ามต่าง ๆ"* — กติกาข้อนี้แก้ตรงนั้น

- **แก้เฉพาะสิ่งที่ถูกรายงาน** เจอปัญหาอื่นระหว่างทาง → **รายงาน ไม่ต้องแก้เอง** รอเจ้าของสั่ง
- **ห้ามลบ/รื้อของที่ทำงานอยู่** เพื่อความสวยงามหรือความสะอาดของโค้ด
  ถ้าจำเป็นต้องรื้อจริง ต้องบอกก่อนว่ารื้ออะไร แลกกับอะไร แล้วรอไฟเขียว
- **ห้ามเพิ่มฟีเจอร์ที่ไม่มีใครขอ** แม้จะดูมีประโยชน์
- แก้เสร็จต้องบอกครบว่า **แตะไฟล์ไหน · อะไรเปลี่ยนพฤติกรรม · อะไรที่ยังไม่ได้แตะ**

## 2. เจอบั๊กแล้วต้องกวาดหาแบบเดียวกันทั้งระบบ

เกิดจริง 2 รอบในวันเดียว: `scrollIntoView` ที่เลื่อนจอตอน mount แก้ที่หน้าผู้ช่วย AI
แล้วไม่ได้ `grep` หาที่อื่น → หน้าแรกพังด้วยอาการเดียวกัน เจ้าของเจอเองอีกรอบ

**ก่อนบอกว่าแก้เสร็จ ต้อง `grep` หารูปแบบเดียวกันในทั้ง `src/` แล้วรายงานว่าเจอกี่จุด**

## 3. บังคับที่โค้ด ไม่ใช่ขอร้องโมเดล

`ask_user` เคยใส่ข้อความบอกโมเดลว่า *"ถามแล้วให้หยุด"* แต่ลูปไม่มี `break`
โมเดลไม่หยุด → ผู้ใช้เห็น "ขอโทษค่ะ ลองพิมพ์ใหม่" พร้อมปุ่มให้กด

**ถ้าต้องการให้หยุด ให้เขียน `break` · ถ้าต้องการให้ห้าม ให้ throw · อย่าเขียน prompt ขอ**
และผู้ใช้ต้องไม่เจอจอเปล่า / ข้อความขอโทษ ในจังหวะที่ระบบทำงานสำเร็จไปแล้ว

## 4. ให้ข้อมูลครบก่อนโทษว่า AI โง่

`get_overview` เคยคืนแต่ยอดรวม → AI ตอบ "202 บาท" แล้วจบ ทั้งที่ผู้ใช้ถามว่า "จ่ายอะไรบ้าง"

**ก่อนแก้ prompt ให้เช็คก่อนว่า tool ส่งข้อมูลพอให้ตอบได้จริงไหม** — คำถามที่ขอ "รายการ"
ต้องได้รายการ ไม่ใช่ตัวเลขรวม

## 5. พูดเฉพาะที่พิสูจน์แล้ว

เกิดจริง: ตรวจ 7 หน้าที่ไม่ต้องล็อกอิน แล้วรายงานว่า *"ไม่มีหน้าไหนล้นแนวนอน"*
ทั้งที่หน้าที่มีปัญหาอยู่หลังล็อกอินซึ่งไม่ได้ตรวจเลย

- **แยกให้ชัดทุกครั้ง: "วัดแล้ว" กับ "อ่านโค้ดแล้วเชื่อว่า"**
- `build` / `typecheck` ผ่าน **ไม่ใช่หลักฐานว่าหน้าใช้ได้** — หน้า `force-dynamic`
  ไม่ถูก render ตอน build
- ยืนยัน deploy ด้วย `curl https://sudochatbot.online/api/health` เทียบ `commit` กับ
  `git rev-parse --short=7 origin/main` เท่านั้น ห้ามใช้ HTTP status
- **ห้ามอ้างว่า 100% เมื่อยังไม่เห็นของจริง** — บอกตรง ๆ ว่าเหลืออะไรที่ยังไม่ได้ยืนยัน
  ดีกว่าเดาแล้วให้เจ้าของไปเจอเอง

## 6. ก่อน push ทุกครั้ง

```bash
npm run verify     # typecheck + ตรวจบัญชี + ด่าน UI + build — ต้องอ่านผลจริง ห้าม grep แล้วเดา
```

เคย push โค้ดที่ build ไม่ผ่านขึ้น production เพราะ `grep` แล้วบรรทัด error ถูกกลืน

**clone ใหม่ต้องเปิดด่าน pre-push ครั้งเดียว** ไม่งั้นข้อนี้เป็นแค่คำสัญญา:

```bash
git config core.hooksPath .githooks
```

hook รัน 3 ด่านเร็ว (5 วินาที) ไม่รัน build เพราะ Vercel ตรวจ build ให้แล้ว
build ไม่ผ่าน = ไม่ deploy ขึ้น production แต่ `check`/`check:ui` ไม่มีใครรันให้เลย

## 7. เรื่องบัญชี-ภาษี

- **กฎถาวร → โค้ด · ประกาศที่มีวันหมดอายุ → ตาราง** (`vat_rates`, `rd_filing_extensions`,
  `th_public_holidays`) พ้นช่วงที่ยืนยันไว้ให้คืน "ไม่รู้" ห้ามเดาต่อ
- **ห้ามแก้ตัวเลขในฐานข้อมูลตรง ๆ เพื่อให้คำเตือนหาย** — ต้องยกเลิก/ออกใหม่ผ่านหน้าเว็บ
  ไม่งั้นเอกสารกับสมุดรายวันแยกกันและไม่มีร่องรอย
- กฎเลือกเอกสารสำหรับ VAT อยู่ที่ `src/lib/vat-docs.ts` ที่เดียว
- ทิศที่ผิดแล้วอันตรายคือ **"ผ่อนปรนเกินจริง"** (เลื่อนกำหนดยื่นออกไป / ลดยอดภาษี)
  เพราะทำให้ผู้ใช้โดนเบี้ยปรับ ส่วนเข้มเกินแค่ทำให้รีบ

## 8. ความปลอดภัยที่ห้ามต่อรอง

- `SUPABASE_SERVICE_ROLE_KEY` ห้ามหลุดถึง client · ห้ามให้ใครวาง token/secret ในแชท
  (ถ้าวางแล้ว = ถือว่าหลุด ต้อง rotate)
- Server Action ต้อง `assertMember(shopId, roles)` ก่อนแตะ service client · หน้า admin ต้อง `is_platform_admin()`
- ห้ามเก็บ IP ดิบ — HMAC-SHA256 เท่านั้น (คอลัมน์ `guest_ai_usage.ip_hash`)
- เพดาน/โควตา/kill switch ตรวจฝั่ง server และ **fail-closed** เสมอ
- ห้ามแตะ Auth config (provider, Site URL, redirect) — เจ้าของทำเอง
  ปิด provider ที่ยังมีบัญชีผูกอยู่ = ล็อกคนนั้นออกถาวร (เกิดจริงกับ Facebook)

## 9. หน้าตา

- ห้ามอิโมจิใน UI (ใช้ Lucide) · ฟอนต์ IBM Plex Sans Thai · อะไรที่มี hover ต้องกดได้จริง
- เป้ากดขั้นต่ำ 44px ในหน้าที่ลูกค้าใช้
- `cn()` เสมอเมื่อ component รับ prop `className` — ต่อสตริงเองทำให้คลาสชนกันเงียบ ๆ
  (เคสโลโก้ซ้อนกัน 2 อันบนมือถือ)
- โครง `loading.tsx` ต้องมีรูปร่างเท่าหน้าจริง โครงที่ไม่ตรงแย่กว่าไม่มีโครง
- ห้ามเอาแบนเนอร์แจ้งเตือนกลับไปไว้ใน `dashboard/layout.tsx` — คำเตือนที่เห็นทุกหน้าคือคำเตือนที่ตาชา

## 10. ภาษา

รายงานเป็นภาษาไทยทั้งหมด · คอมเมนต์ในโค้ดเขียนว่า **"ทำไม"** ไม่ใช่ "ทำอะไร"
โดยเฉพาะจุดที่เคยพังมาแล้ว ให้เขียนกำกับไว้ว่าพังเพราะอะไร กันคนถัดไปแก้กลับ
