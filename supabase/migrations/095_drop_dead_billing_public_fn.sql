-- ============================================================
--  095 — ลบ platform_billing_public() ที่ตายแล้ว
--
--  ⚠️ บั๊กชนิดที่มองไม่เห็นจนกว่าจะมีคนเรียก (พบ 6 ส.ค. 2569 จาก Supabase advisor)
--
--  migration 093 ลบคอลัมน์ payment_gateway และ omise_public_key ทิ้ง
--  แต่ Postgres **ไม่ได้** ตรวจ body ของฟังก์ชัน LANGUAGE sql ตอน drop column
--  (ผูก dependency เฉพาะ body แบบ BEGIN ATOMIC เท่านั้น) ฟังก์ชันจึงยังอยู่
--  แล้วพังตอน "ถูกเรียก" ไม่ใช่ตอน "ถูกทำให้พัง" — วัดแล้ว: เรียกทีไรได้
--  ERROR 42703 column "payment_gateway" does not exist ทุกครั้ง
--
--  ทำไมลบ ไม่ใช่แก้ให้ถูก:
--   · ไม่มีโค้ดใน src/ เรียกใช้เลยสักที่ (grep แล้ว) เป็นซากยุค Omise
--   · มันคืนค่าตั้งค่าระดับแพลตฟอร์มให้ "ผู้ใช้ที่ล็อกอินคนไหนก็ได้" อ่าน
--     ซึ่งไม่มีเหตุผลรองรับแล้วเมื่อไม่มีใครใช้ — ของที่ไม่ได้ใช้แต่เปิดสิทธิ์ไว้
--     คือช่องที่รอวันมีคนเจอ
--
--  ด่านกันซ้ำ: scripts/migration-dryrun.sh ตรวจ body ของทุกฟังก์ชันหลังรัน migration ครบ
--  โดยสั่งสร้างใหม่จากนิยามเดิม — ตัวไหน body อ้างของที่ไม่มีแล้วจะ error ทันที
-- ============================================================
drop function if exists public.platform_billing_public();
