node_modules/
.next/
out/
.env*
!.env.example
.vercel
*.tsbuildinfo
.DS_Store

# ไฟล์สำรองมีข้อมูลลูกค้าจริง ห้ามขึ้นกิตเด็ดขาด
backups/
.restore-tmp.sql

# Playwright
test-results/
playwright-report/

# APK ที่ build แล้ว — ไม่ commit ขึ้น repo (ไฟล์ใหญ่ + สร้างใหม่ได้เสมอ)
mobile/build/
android/local.properties

# ภาพหน้าจอที่ถ่ายไว้ตรวจงาน ไม่ใช่ของที่ต้องเก็บใน repo
shots/
test-results/

# ไฟล์ตรวจงานชั่วคราวของผู้ช่วย — ภาพหน้าจอหลังล็อกอินมีข้อมูลบัญชีทดสอบ ห้ามขึ้น repo สาธารณะ
audit-shots/
*.probe.mjs
audit.mjs
vshot.mjs
