# 🚀 SudoChatBot (ระบบบัญชี AI) — เอกสารส่งต่อ

> เปิด repo นี้ใน Claude Code แล้วสั่ง "อ่าน HANDOFF.md แล้วทำงานที่ค้างต่อ" ได้เลย
> อัปเดตล่าสุด: 2026-07-29 — **บัญชีผู้ใช้มีที่ระบุตัวตนแล้ว + วันหยุดราชการ 2569** (commit `008004e`)
> ก่อนหน้า: 2026-07-26 ยกเครื่อง UI/UX ทั้งระบบ (`1696bd9`)
> ก่อนหน้า: 2026-07-23 PIVOT ครั้งใหญ่ จาก AI Chatbot ขายของ → ระบบบัญชีครบวงจร (ชื่อ/โดเมนเดิม)

## 🌙 2026-07-30 — เก็บกวาดใหญ่ก่อนโหมดเฝ้ายามข้ามคืน

**เจ้าของทำเสร็จ:** สำรองข้อมูลขึ้น Google Drive (npm run backup) · รหัสผ่านขั้นต่ำ 8 ·
ปิด Facebook provider · ลบ edge functions ทั้งหมด

**โค้ดวันนี้ (สำคัญ):**
- เอกสารกฎหมาย 3 หน้าตรงกับระบบจริง + ปุ่มออกจากหน้า (`a8f8d02`)
- เอาปุ่ม Facebook ออกจาก login/signup (`1b4de50`) — ⚠️ provider ปิดแล้วแต่ยังมี
  บัญชี `ta_free14@hotmail.com` ผูก facebook อย่างเดียว ไม่มีรหัสผ่าน = เข้าไม่ได้
  จนกว่าจะตั้งรหัสผ่าน (ผ่าน /forgot-password) หรือลบทิ้ง
- โลโก้ซ้อนบนมือถือ: Logo ต้องใช้ cn() ไม่ใช่ต่อสตริง (`8b837f4`) + ด่าน check:ui (`3de1db7`)
- แชท AI: smart scroll + หน้าต้อนรับการ์ดงาน 6 ใบ (`27321b7`, `a85c2e7`)
- รอบ 1 ล้างความรก: 3 แบนเนอร์ออกจาก layout ไปอยู่หน้าภาพรวมที่เดียว (`9d3f08c`)
  ⚠️ ห้ามเอากลับไปไว้ใน layout — รอบ 2 (กระดิ่งกล่องจดหมาย) รอเจ้าของอนุมัติ
- ไฟล์ยื่นสรรพากร: ตัวประกอบบรรทัดย้ายไป src/lib/rd.ts + เทสต์ระดับบรรทัด (`5a7c872`)
- npm run backup สำรองทุกตารางเป็น JSON (แพ็กฟรี Supabase ไม่มี backup ให้)
- Supabase usage ตรวจแล้ว: egress <1% — **ไม่ต้องอัป Pro** แบนเนอร์เป็น Fair Use ทั่วไป

**ค้างฝั่งเจ้าของ (ปรับปรุง 30 ก.ค. 2569 บ่าย):**
1. 🔴 ทดสอบไฟล์ .txt กับโปรแกรม RD Prep จริง — ก่อน 7 ส.ค. (เจ้าของโหลดโปรแกรมแล้ว)
2. 🔴 Supabase Site URL ยังเป็น vercel.app → เปลี่ยนเป็น https://sudochatbot.online
   ที่ `auth/url-configuration` + เพิ่ม `https://sudochatbot.online/**` ใน Redirect URLs
   **Claude ทำแทนไม่ได้** — MCP ไม่มีคำสั่งแตะ auth config, ไม่มี SUPABASE_ACCESS_TOKEN ใน .env.local,
   ไม่มี Chrome ที่ล็อกอิน Supabase ต่ออยู่ · ฝั่งโค้ดแก้ให้แล้วด้วย `authOrigin()` (`429040f`)
3. 🟠 ยกเลิกเอกสาร 2 ใบผ่านหน้าเว็บ (ห้าม SQL) — ตรวจแล้วว่ากดได้ปลอดภัย ไม่ติดล็อกงวด:
   - กิจการ **King** (`justin.minnie89@gmail.com` = ของเจ้าของเอง) `EXP-2026-0003` 54 บาท ซ้ำกับ `EXP-2026-0002`
     `/dashboard/expenses/d5c700e1-9e53-4f85-a0f4-dda165b31143`
   - กิจการ **ทีเด็ดอาหารปลา** (`jj@gmail.com` — ต้องล็อกอินบัญชีนั้น) `EXP-2026-0003` วันที่ 2069-06-19
     63,750 บาท สถานะ awaiting · ยกเลิกแล้วออกใหม่วันที่ 19/06/2026
     `/dashboard/expenses/1fb01b05-8fbf-4477-9321-be591346ee3b`
   - ระบบไม่มีปุ่มแก้ไขเอกสารที่ออกแล้ว (มีแค่ voidDoc / credit-debit note) จึงต้องยกเลิก+ออกใหม่เท่านั้น
4. 🟡 Rotate LINE token (วางใน /dashboard/admin เท่านั้น ห้ามวางในแชท)
5. 🟡 สมัครบัญชีทดสอบ + ใส่ TEST_EMAIL/TEST_PASSWORD ใน .env.local ให้ Claude ตรวจหลังล็อกอินได้
6. 🟡 บัญชี `ta_free14@hotmail.con` (พิมพ์ .con ผิด) ยังอยู่ — มีรหัสผ่านแต่กู้ทางอีเมลไม่ได้
   ถือกิจการ "Sudo" ที่ว่างเปล่า (0 เอกสาร) จะลบก็สั่งได้

**เสร็จแล้ววันนี้ (30 ก.ค. บ่าย):**
- ลบ `ta_free14@hotmail.com` + กิจการ "admin" ตามคำสั่งเจ้าของ — 4 เอกสารตัวอย่าง 0 เอกสารจริง
  ก่อนลบต้อง null `ai_settings.updated_by` (4) และ `ai_provider_keys.updated_by` (2) เพราะ FK เป็น NO ACTION
  หลังลบ: platform admin เหลือ `justin.minnie89@gmail.com` (ยังเข้าหน้า admin ได้), คีย์ AI ครบ 2 ตัว,
  ทดสอบ AI จริงผ่าน guest endpoint แล้วตอบปกติ, ไม่มีกิจการ/สมาชิกกำพร้า
- ✅ backup: เจ้าของรัน + ก๊อปขึ้น Google Drive แล้ว · Claude รันเพิ่มก่อนลบข้อมูล → `backups/2026-07-30/` 25 ตาราง 886 แถว
- ด่าน pre-push ที่บล็อกได้จริง (`012c203`) + `CLAUDE.md` โหลดอัตโนมัติ (`d1034a6`)

---

## 👤 2026-07-29 — บอกให้ได้ว่า "ตอนนี้ใครล็อกอินอยู่" + วันหยุดราชการ 2569

### เดิมทั้งเว็บไม่มีสักจุดเดียวที่บอกว่าใครล็อกอินอยู่

ผู้ใช้แจ้งเอง มีแค่ปุ่ม "ออกจากระบบ" แปลว่าวิธีเดียวที่จะรู้ว่าตัวเองเป็นบัญชีไหน
คือกดออกจากระบบแล้วดูตอนล็อกอินใหม่ — ราคาที่แพงเกินไปสำหรับคำถามง่าย ๆ

**ทำไมหนักกว่าเว็บทั่วไปสำหรับระบบบัญชี:** ทุกเอกสารที่บันทึก/ยกเลิกถูกประทับ user id
ลง audit log และลบไม่ได้ ถ้านั่งเครื่องที่ค้างบัญชีคนอื่นไว้ ร่องรอยจะชี้ไปผิดคนถาวร
ยิ่งสำนักงานบัญชีที่มีหลายบัญชี และ SME ที่ใช้คอมร่วมกัน ยิ่งต้องเช็คได้ก่อนลงมือ

| ที่ | เห็นอะไร |
|---|---|
| ท้ายแถบเมนูซ้าย | รูปวงกลม + ชื่อ + **อีเมล** เห็นโดยไม่ต้องกด · พับเมนูเหลือวงกลม + tooltip |
| หัวมือถือ | รูปวงกลม (มือถือคือเครื่องที่คนสลับบัญชีบ่อยที่สุด) |
| `/dashboard/account` | แก้ชื่อ/เบอร์ · อีเมลอ่านอย่างเดียว · กิจการที่เข้าถึงได้ + สิทธิ์ · ขอลิงก์ตั้งรหัสใหม่ |

ปุ่มออกจากระบบย้ายเข้าไปอยู่ในเมนูบัญชี ซึ่งเป็นที่ที่ควรอยู่ และกดพลาดยากขึ้น

### ตัดสินใจด้านความปลอดภัย (อย่าย้อนกลับ)

- **ไม่ให้แก้อีเมลเอง** เป็นทั้งกุญแจล็อกอินและตัวระบุใน audit log การเปลี่ยนต้องยืนยันสองทาง
- **ไม่มีช่องตั้งรหัสผ่านใหม่ในหน้าที่เปิดค้างได้ทั้งวัน** บังคับผ่านลิงก์ทางอีเมลเท่านั้น
  ไม่งั้นใครเดินมานั่งต่อก็ยึดบัญชีได้โดยไม่ต้องรู้รหัสเดิม
- `updateMyProfile` ใช้ client ปกติ **ไม่ใช่ service role** และไม่รับ id จาก client เลย

### กับดักสองอันที่เกือบพลาดซ้ำรอบเดิม

1. **`roleLabel` อยู่ในไฟล์ `"use client"` แล้ว Server Component เรียก**
   → build ผ่านสบาย พังตอนรันจริง เพราะหน้าเป็น `force-dynamic` จึงไม่ถูก render ตอน build
   ย้ายไป `src/lib/roles.ts` **กฎ: ฟังก์ชันที่ทั้งสองฝั่งใช้ ต้องไม่อยู่ในไฟล์ที่มี `"use client"`**
2. **เมนูต้อง portal ออก `document.body`** หัวมือถือเป็น `sticky z-20` = stacking context
   ของที่ render ข้างในจะโดนปุ่มลอยทับ (บั๊ก "ปุ่มค้าง" ตัวเดียวกับตัวสลับกิจการ)

### วันหยุดราชการ 2569 — เข้าตาราง `th_public_holidays` แล้ว (23 วัน)

กระทบกำหนดยื่นจริง **2 จุด** (ตรวจแล้วทั้งปี ไม่ใช่ใส่ไปเฉย ๆ)

| งวด | ตามกฎหมาย | เลื่อนเป็น | เพราะ |
|---|---|---|---|
| พ.ย. 2569 (กระดาษ) | 7 ธ.ค. | **8 ธ.ค.** | ชดเชยวันพ่อ (5 ธ.ค. ตรงเสาร์) |
| มี.ค. 2569 (ออนไลน์) | 15 เม.ย. | **16 เม.ย.** | สงกรานต์ |

- **ไม่ใส่วันหยุดสถาบันการเงินที่ ธปท. ประกาศเพิ่ม** (เช่น 16 ต.ค. 2569 เฉพาะ กทม.)
  เพราะสรรพากรเปิดทำการ — ทิศทางที่ผิดแล้วอันตรายคือ **"เลื่อนเกินจริง"**
  ผู้ใช้จะยื่นไม่ทันแล้วโดนเบี้ยปรับ ส่วน "เลื่อนน้อยไป" แค่ทำให้รีบเกินซึ่งไม่เสียหาย
- **ยังไม่ใส่ปี 2570** แหล่งที่หาได้ขัดกันเอง (ระบุชดเชยวันปิยมหาราชเป็น 26 ต.ค. 2570
  ซึ่งเป็นวันอังคาร ทั้งที่ 23 ต.ค. ตรงเสาร์ต้องชดเชยจันทร์ 25) และขาดวันฉัตรมงคล/อาสาฬหบูชา
  ระบบจึงยังขึ้นว่า "ยังไม่ได้กรอกวันหยุดของปีนี้" ซึ่งถูกต้องกว่าการเดา

### ของที่หลุดจากคอมมิตแก้ CLS เมื่อวาน

โครงกระดูกวาดปุ่มมุมขวาไว้ทุกหน้า ทั้งที่ **5 หน้าไม่มีปุ่มนั้น**
(ทรัพย์สิน · แพ็กเกจ · คู่มือ · การเงิน · ตั้งค่า) บนมือถือปุ่มเป็น `w-full` สูง 44px
พอโหลดเสร็จเนื้อหาเลื่อนขึ้น — เพิ่ม prop `action` ให้ `Head` แล้วไล่ตั้งตามหน้าจริง

---

## 🧾 2026-07-27 — รอบตรวจซ้ำ + ทดสอบ UI แบบผู้ใช้จริง

**วิธีที่ได้ผลรอบนี้:** เลิกตรวจแค่ระดับโค้ด/ฐานข้อมูล แล้วเปิดเว็บ production
ด้วยเครื่องมือเบราว์เซอร์จริง + อ่านไฟล์ Excel ที่ผู้ใช้โหลดออกไปจริง
บั๊กที่เจอรอบนี้ **ไม่มีตัวไหนที่ typecheck/build/SQL จับได้เลย** เพราะไม่มีอะไร "พัง"

### บั๊กที่ไม่มีใครเห็นมาก่อน

| เจอ | ผลจริงที่เกิดไปแล้ว |
|---|---|
| **ไม่มีระบบรีเซ็ตรหัสผ่านทั้งระบบ** | ลูกค้าลืมรหัส = เข้าข้อมูลบัญชีตัวเองไม่ได้ถาวร |
| **ภาษีขายนับซ้ำข้ามเดือน** | ใบแจ้งหนี้ มิ.ย. + ใบเสร็จ ก.ค. นับ VAT สองรอบ |
| **ชุดส่งสำนักงานบัญชีรวมใบเสนอราคา** | ภาษีขายเกินจริง |
| **งบทดลองในไฟล์เป็นยอดเคลื่อนไหว แต่พาดหัวว่ายอดคงเหลือ** | นักบัญชีทำงบต่อผิดทั้งงบ |
| **ไฟล์ยื่นสรรพากรถูกส่งไปเบราว์เซอร์ทุกแพ็ก** | ล็อกเป็นแค่ UI เปิด devtools ก็ได้ไฟล์ |
| **เอกสาร 63,750 บาท ลงวันที่ปี 2069** | หายจากรายงานทุกงวด แต่ค้างในเจ้าหนี้ตลอดไป |
| **แถวรวมเขียน "รวมเดือน 2026-07" แต่รวม 6 เดือน** | อ่านว่ากำไร 14,300 ทั้งที่ขาดทุน 10,700 |
| **ลิงก์ท้ายหน้าสูง 16px บนมือถือ** | กดพลาด |
| **ปุ่มไอคอนล้วนไม่มีชื่อ** | โปรแกรมอ่านหน้าจออ่านได้แค่ "ปุ่ม" |

### กติกาที่ได้จากรอบนี้ (ห้ามละเมิด)

1. **กฎที่ deterministic → โค้ด · ข้อมูลที่ประกาศเป็นปี ๆ → ตาราง**
   ม.52 (ยื่นภายใน 7 วัน) อยู่ในโค้ด · "+8 วันยื่นออนไลน์" อยู่ใน `rd_filing_extensions`
   เพราะมาจากประกาศที่มีวันหมดอายุ เหมือน `vat_rates`
   **ผมเผลอฮาร์ดโค้ด "+8 วัน" ไปแล้วครั้งหนึ่ง จับได้เองก่อน deploy รอบสอง**
2. **พ้นช่วงที่ยืนยันไว้ = ไม่เดาต่อ** คืน null แล้วบอกให้คนไปตรวจประกาศ
   การบอกกำหนดช้ากว่าจริง = ผู้ใช้ยื่นสาย = เงินเพิ่ม 1.5%/เดือน แย่กว่าไม่บอก
3. **นับถอยหลังยึดวันที่เร็วที่สุดเสมอ** บอกให้รีบดีกว่าบอกให้ชะล่าใจ
4. **กฎเลือกเอกสารต้องอยู่ที่เดียว** → `src/lib/vat-docs.ts` รายงานต้องตรงสมุดรายวัน
5. **ยืนยัน deploy ด้วย `curl .../api/health` เทียบ commit** ห้ามใช้ status code

### เครื่องมือใหม่

- `npm run verify` — 6 หมวด 400,000+ เคส (ปัดเศษ · ค่าเสื่อม · ไฟล์ RD · ตัวตรวจก่อนโหลด · วันที่ · อัตรา WHT)
- `/api/health` คืน `commit` + `branch`
- `src/components/date-field.tsx` — ช่องวันที่กลาง อ่านเป็นไทย+พ.ศ. กันกรอกปีผิด
- `src/app/dashboard/data-health.tsx` — แถบเตือนข้อมูลไม่ครบบนหน้าภาพรวม
- `docs/REMOVED-CHATBOT-ARTIFACTS.md` — ของยุคแชทบอทที่ลบไปแล้ว + edge function ที่ยังค้าง

### ⚠️ ค้างที่เจ้าของเท่านั้น

1. ~~**อัปเกรด Supabase เป็น Pro**~~ — ❌ **ยกเลิกข้อนี้แล้ว** ตรวจ usage จริง 30 ก.ค.: egress 0.045/5 GB · DB 77/500 MB · MAU 20/50,000 ไม่เกินโควตาสักตัว **ห้ามจ่าย Pro**
2. ~~เปิด Leaked Password Protection~~ — ❌ **ฟีเจอร์นี้เฉพาะแพ็ก Pro** ใช้ความยาวรหัสผ่านขั้นต่ำ 8 แทนแล้ว
3. แก้ 2 เอกสาร: `EXP-2026-0003 (มานิตย์ฟาร์ม)` วันที่ปี 2069 · `CJ 1446` ซ้ำ 2 ใบ — **กดยกเลิกในระบบ ห้ามลบด้วย SQL**
4. ทดสอบไฟล์ `.txt` กับ RD Prep จริง แล้วบันทึกเวอร์ชันที่ทดสอบ
5. Rotate LINE token (platform OA + Login channel มีอยู่จริง) · ลบ edge function 8 ตัว

---

## 🚨 อ่านก่อนทำต่อ — เส้นทางที่ยังไม่เคยรันจริงกับผู้ใช้ (2026-07-26)

ทุกอย่างด้านล่างขึ้น production แล้วและ build ผ่าน แต่ยัง **ไม่เคยมีคนล็อกอินเข้าไปกดจริง**
(เครื่อง dev ไม่มี `.env.local` จึงรันในเครื่องไม่ได้ ตรวจได้แค่ระดับ HTTP กับตรรกะแยกส่วน)

| เส้นทาง | ตรวจแล้วแค่ไหน | ต้องทดสอบอะไรต่อ |
|---|---|---|
| `/api/sheet` (build/parse) | HTTP 401 กับคนไม่ล็อกอิน · exceljs เขียน/อ่านไฟล์ผ่านบน Node ภาษาไทยไม่เพี้ยน | กดปุ่ม Excel ในหน้ารายงานจริง |
| `/api/sheet/accountant` | HTTP 401 · ตรรกะประกอบชีตยังไม่เคยรัน | กดปุ่ม "ชุดส่งนักบัญชี" แล้วเปิดไฟล์ดู 7 แท็บ |
| นำเข้า statement PDF | `unpdf` มี `extractText`/`getDocumentProxy` ตรงตามที่เรียก · regex แยกแถวถูกต้องกับข้อความตัวอย่าง | อัป PDF จริงจากแอปธนาคาร |
| รูปย่อในประวัติแชท | ยังไม่เคยรัน | ส่งรูปบิล → รีเฟรช → ต้องยังเห็นรูป |
| สาขาบนใบกำกับภาษี | ยังไม่เคยเรนเดอร์ | กรอกสาขาแล้วพิมพ์ใบเสร็จที่คิด VAT |
| คำเตือน ม.86/4 | ยังไม่เคยเรนเดอร์ | ลบเลขผู้เสียภาษีออกแล้วกดพิมพ์ ต้องขึ้นแถบแดง |

**ความเสี่ยงที่รู้ตัวและยอมรับไว้**
- `createImageBitmap` ไม่มีในเบราว์เซอร์เก่ามาก (iOS < 15) → รูปย่อไม่ขึ้น แต่ไม่ทำให้ส่งไฟล์ล้ม (มี try/catch)
- ชุดส่งนักบัญชีจำกัด 5,000 เอกสาร / 5,000 รายการบัญชีต่องวด ชนเพดานแล้วขึ้นเตือนในไฟล์ว่าข้อมูลไม่ครบ ห้ามใช้ยื่น
- `VAT_RATE` รองรับเฉพาะอัตราจำนวนเต็มเปอร์เซ็นต์ (`Math.round(VAT_RATE*100)`) ถ้าวันหนึ่งเป็น 7.5% ต้องแก้สูตร

## 🚨 2026-07-26 — เหตุการณ์: หน้าภาพรวมล่มบน production (แก้แล้ว `45e031d`)

**อ่านก่อนแตะ `dashboard/layout.tsx` หรือย้ายอะไรข้ามเส้น server/client**

| | |
|---|---|
| **อาการ** | เข้า `/dashboard` แล้วจอว่าง ขึ้น "เกิดข้อผิดพลาดชั่วคราว" ทั้งเดสก์ท็อปและมือถือ · ไม่ล็อกอินไม่พัง |
| **สาเหตุ** | ส่ง `nav`/`ADMIN_NAV` (มี `icon` เป็น React component) จาก layout ที่เป็น **Server Component** ไปเป็น prop ของ `SideNav` ที่เป็น **Client Component** — React serialize ฟังก์ชันข้ามเส้นไม่ได้ |
| **ทำไมหลุด** | หน้านี้เป็น `force-dynamic` จึง **ไม่ถูก render ตอน build** — `next build` + `tsc` ผ่านทั้งคู่ |
| **ทำไมเงียบ** | `error.tsx` ของ segment **ไม่จับ error ของ `layout.tsx` ตัวเอง** → เด้งขึ้น `app/error.tsx` ซึ่งตอนนั้นไม่ log อะไรเลย |
| **แก้** | ย้าย `NAV`/`ADMIN_NAV` เข้าไปใน `side-nav.tsx` (client) · layout ส่งแค่ `isAdmin` boolean |

**กติกาที่ต้องยึดจากนี้**
1. **ห้ามส่งฟังก์ชันหรือ React component ข้ามเส้น Server → Client** ส่งได้แค่ข้อมูลธรรมดา / server action / JSX element
2. **`force-dynamic` = build ยืนยันอะไรไม่ได้เลย** ต้องเปิดหน้าจริงในสถานะที่ตรงกับผู้ใช้ (ล็อกอินแล้ว)
3. **HTTP status จากภายนอกไม่ใช่หลักฐาน** — `/dashboard` ตอบ 307 เพราะ redirect ก่อนถึงโค้ดที่พังด้วยซ้ำ อย่าเอามาสรุปว่า deploy สำเร็จ
4. **แก้ที่แตะ layout หรือเส้น server/client → deploy แยกก้อน** ให้ยืนยันทีละอย่าง
5. ตอนนี้ `app/error.tsx` และ `dashboard/error.tsx` **ยิงรายงานเข้า `/api/log-error` แล้ว** เก็บใน `audit_logs` (`action = 'client_error'`) — ตรวจได้ด้วย SQL ทันทีเมื่อมีคนแจ้งว่าพัง

## ⚖️ 2026-07-26 — เอกสารภาษีถูกกฎหมาย + ถอด xlsx (commit `5d32b45`)

**ตรวจด้วยกรอบทดสอบการควบคุมแบบ SOX แต่จับกับกฎหมายที่บังคับจริงในไทย** (SOX เป็นกฎหมายสหรัฐฯ ไม่ได้บังคับกับ SME ไทย)

**Material Weakness ที่พบและแก้แล้ว:**
| # | ปัญหา | ผลกระทบจริง |
|---|---|---|
| TAX-01 | ใบกำกับภาษีไม่มี "สำนักงานใหญ่/สาขาที่ ..." ทั้งสองฝั่ง | **ผู้ซื้อขอคืนภาษีซื้อไม่ได้** (ประกาศอธิบดีฯ 199 บังคับตั้งแต่ 1 ม.ค. 2558) |
| TAX-02 | ระบบพิมพ์ใบกำกับภาษีที่ข้อมูลไม่ครบ ม.86/4 เงียบ ๆ | ออกใบที่ใช้ไม่ได้โดยไม่มีใครรู้ |
| TAX-03 | 50 ทวิ + ไฟล์ยื่นฮาร์ดโค้ด "ค่าสินค้า/บริการ" และสาขา "00000" | ยื่น ภ.ง.ด.3/53 ผิดทุกบรรทัด |

**ไฟล์กลางใหม่ `src/lib/tax-th.ts` — กฎภาษีทั้งหมดอยู่ที่นี่ที่เดียว ห้ามเขียนซ้ำที่อื่น**
- `branchLabel()` ข้อความบนเอกสาร · `branchCode()` รหัส 5 หลักสำหรับไฟล์ยื่น (คนละรูปแบบ)
- `isJuristicPerson()` / `rdFormFor()` — กฎแยก ภ.ง.ด.3 vs 53 (**เคยเขียนซ้ำ 2 ที่ ถ้าเพี้ยนกัน = เอกสารกับแบบที่ยื่นไม่ตรงกัน**)
- `WHT_INCOME_TYPES` ประเภทเงินได้ ม.40 · `WHT_PRESETS` อัตราที่ใช้บ่อย (เป็นตัวช่วยกรอก ไม่ใช่คำวินิจฉัยภาษี)
- `checkTaxInvoice()` ตรวจครบตาม ม.86/4 ก่อนพิมพ์

**Migration ที่ apply บน production แล้ว:** `shops.branch` (default 'สำนักงานใหญ่'), `fin_docs.contact_branch`, `fin_docs.wht_income_type`

**ถอด `xlsx` ออกทั้งโปรเจกต์ → `/api/sheet` ฝั่งเซิร์ฟเวอร์**
- xlsx@0.18.5 มีช่องโหว่ระดับสูงและไม่มี patch (SheetJS ออกจาก npm แล้ว)
- ⚠️ **บทเรียน: อย่าเอา exceljs ไปไว้ฝั่ง client** — วัดได้จริงว่าลากโค้ด zip ของ Node ติดไป **912 KB** ต่อผู้ใช้ทุกคน
- ตอนนี้ทุกอย่างทำที่ `/api/sheet` (build/parse) → client โหลดเพิ่ม **0 KB** ยืนยันจาก build แล้ว
- `downloadWorkbook()` รองรับหลายแท็บในไฟล์เดียว — พร้อมต่อยอดเป็น "ชุดส่งสำนักงานบัญชี"

**⚠️ ยังไม่ได้ทำ / ต้องให้ผู้เชี่ยวชาญยืนยัน:**
- รูปแบบไฟล์ RD Prep (.txt) **ยังไม่เคยทดสอบกับโปรแกรม RD Prep จริง** — ต้องให้ผู้ทำบัญชีลองนำเข้าก่อนใช้ยื่นจริง
- ยังไม่มีปุ่ม "ดาวน์โหลดชุดส่งสำนักงานบัญชี" ที่รวมทุกรายงานของงวดในไฟล์เดียว (`downloadWorkbook` พร้อมแล้ว เหลือต่อ UI)
- VAT 7% ฮาร์ดโค้ดหลายที่ ถ้าอัตราเปลี่ยนต้องไล่แก้ — ควรย้ายเข้า `tax-th.ts`
- ผู้เขียนโค้ดไม่ใช่ผู้สอบบัญชีรับอนุญาต ทุกอย่างในหมวดนี้ควรให้ CPA ตรวจก่อนใช้อ้างอิงจริง

## 🎨 2026-07-26 — ยกเครื่อง UI/UX (commit `1696bd9`)

**โจทย์จากเจ้าของ:** "ห้ามมีจุดไหนที่ดูออกว่า AI ทำ · มืออาชีพแบบ Gen Z แต่เนี๊ยบ เข้าถึงทุกวัย · กดแล้วต้องมี action ทุกจุด"

**กติกาหน้าตาที่ต้องยึดต่อจากนี้ (ห้ามหลุด):**
| ด้าน | กติกา |
|---|---|
| อิโมจิ | **ห้ามใช้ใน UI เด็ดขาด** — ใช้ Lucide เท่านั้น · ยกเว้นข้อความที่ส่งเข้า LINE (เป็นธรรมชาติของแชท) |
| `Sparkles` | **ห้ามใช้** — เป็นสัญลักษณ์ที่บอกว่า "AI generate" ชัดที่สุด เลือกไอคอนที่สื่อความหมายจริงแทน |
| ไอคอน | Lucide เส้น 1.5 · ในหัวข้อการ์ดใช้ `CardTitleIcon` (กล่อง 32px พื้นเทา) |
| ฟอนต์ | `IBM_Plex_Sans_Thai` — ห้ามเปลี่ยนกลับ Noto (ตัวเลขไม่กว้างเท่ากัน คอลัมน์เงินเบี้ยว) |
| สี | เทา/ขาวเป็นฐาน เขียว emerald เป็น accent เดียว ใช้เฉพาะจุดสำคัญ |
| Empty state | ต้อง**สอนงาน** ด้วย `steps` 3 ข้อ ห้ามบอกแค่ "ไม่มีข้อมูล" |
| อะไรที่มี hover | ต้องกดได้จริง — ไม่งั้นตัด hover ออก |

**component ใหม่ที่ต้องใช้ซ้ำ (อย่าเขียนใหม่):**
- `src/components/row-link.tsx` — แถวตารางกดได้ทั้งแถว (ยังต้องคง `<Link>` บนคอลัมน์แรกไว้ให้คีย์บอร์ด)
- `src/components/toast.tsx` — `useToast()` popup + ปุ่มเลิกทำ (mount ที่ dashboard layout แล้ว)
- `src/components/skeleton.tsx` — `ListSkeleton` / `DashboardSkeleton` / `FormSkeleton` ใช้ใน `loading.tsx`
- `src/components/ui.tsx` — เพิ่ม `CardTitleIcon`, `BackLink`, `StatCard(href)`, `EmptyState(steps/secondary)`
- `src/app/dashboard/command-palette.tsx` — Ctrl+K ค้นเอกสาร/ผู้ติดต่อ/เมนู
- `src/app/dashboard/nav-shell.tsx` — context สถานะพับเมนู (`useNav()`) ใช้ร่วมกันระหว่าง sidebar กับ main

**ของใหม่เชิงฟังก์ชัน:**
- นำเข้ารายการเดินบัญชีรองรับ **PDF** — `src/app/dashboard/money/statement-actions.ts` (แกะฝั่งเซิร์ฟเวอร์ด้วย `unpdf`, ไม่มี worker ให้พัง) · อ่านมาเสนออย่างเดียว ผู้ใช้กดยืนยันรายแถวเสมอ
- **ตัวจับผิด OCR** ใน `doc-form.tsx` — ทานยอดท้ายบิลที่ AI อ่านได้ กับผลรวมที่ฟอร์มคำนวณเอง ต่างเกิน 1 บาท = ขึ้นเตือนแดง
- สมุดรายวันอ่านออกโดยไม่ต้องรู้บัญชี — คำแปลภาษาคน + ป้ายประเภทบัญชี + ลิงก์เอกสารต้นทาง + ตรา "ลงตัว"
- หน้าตั้งค่าแยก 4 แท็บผ่าน URL (`?s=business|payment|notify|team`)

**⚠️ ยังค้าง (subagent ตรวจเจอ แต่ยังไม่ได้แก้):**
- แถวตารางที่ยังไม่ได้ใช้ `RowLink`: `dashboard/page.tsx:171`, `money/page.tsx:108`, `reports/page.tsx:242,333,416`, `products/page.tsx:61`
- `/dashboard/billing/receipt/[id]` เป็น **orphan route** — ไม่มีที่ไหนลิงก์ไป ควรลิงก์จากแถวประวัติเติมเงิน `billing/page.tsx:104`
- การ์ดสรุปในหน้ารายงาน หน้าตาเหมือน `StatCard` ที่กดได้ แต่กดไม่ได้ — ควรใส่ `href` หรือเปลี่ยนหน้าตา
- `admin/shops/shop-row.tsx:50,68` — `<Select onChange>` เปลี่ยนแพ็ก/สถานะลูกค้าทันทีโดยไม่ถามยืนยัน (เสี่ยงบนมือถือ)
- `admin/ai-center.tsx:208,263` — ปุ่มลบ key ไม่มี disabled ระหว่างรอ กดรัวได้
- `useToast()` ต่อสายแล้วเฉพาะ statement import — ที่อื่นยังไม่มี feedback ตอนบันทึกสำเร็จ
- ปุ่มใน `finance/doc-actions.tsx` ยังเป็น `size="sm"` (32px) บนมือถือ รวมถึงปุ่มบันทึกเงิน

## 📍 ที่อยู่ทุกอย่าง

| สิ่ง | ที่อยู่ / ID |
|---|---|
| **เว็บใช้งานจริง** | 🟢 https://sudochatbot.online · alias `sudochatbot-org.vercel.app` |
| **GitHub repo** | `github.com/SPNpeet/SudoChatBot-ORG` branch `main` = production (auto-deploy ทุก push) |
| **Vercel project** | `sudochatbot-org` (team `suphanats-projects`) region `sin1` |
| **Supabase project** | ref `uafnpbawajgonarvlurj` · ap-southeast-1 · Free plan |
| **แผนที่โค้ด** | `docs/REPO-MAP.md` (119 ไฟล์ ~11,000 บรรทัด — regenerate: `node ~/.claude/skills/ast-repo-mapper/scripts/repo-map.mjs src docs/REPO-MAP.md`) |

## 🔄 Pivot 2026-07-23 — เกิดอะไรขึ้น

**ลบทิ้งทั้งหมด (ตามคำสั่งเจ้าของ):** แชทบอทตอบลูกค้า FB/IG/LINE/TikTok, playground, ยิงแอด AI, คลังความรู้บอท, ออเดอร์จากแชท, edge functions ทุกตัว (webhook-*, queue-worker, doc-processor, slip-verifier, ads-watchdog) และ cron ที่ปลุกมัน (unschedule แล้วบน production: kick_queue_worker, kick_doc_processor, kick_slip_verifier, kick_ads_watchdog)
- ⚠️ ตาราง DB เก่าของแชทบอท (conversations, messages, channels, knowledge_* ฯลฯ) **ไม่ได้ drop** — เก็บข้อมูลไว้เฉยๆ ไม่มีผลกับระบบใหม่ · Edge functions บน Supabase ยัง deploy ค้างไว้แต่ไม่มีอะไรเรียก — ลบได้จาก Supabase dashboard เมื่อสะดวก

**สิ่งที่เก็บไว้:** Auth (email/Google/Facebook), multi-tenant `shops` + RLS + RBAC, billing เครดิต/แพ็กเกจ (PromptPay topup + ตรวจสลิป), ศูนย์ AI (8 ค่าย, key ใน Vault), หน้า admin ทั้งชุด, สินค้า + นำเข้าไฟล์, ai-errors, feedback, audit logs

## ✅ ระบบใหม่ทั้งหมด (สร้างเสร็จ + build ผ่าน)

**Migrations 050-054 (apply บน production ครบแล้ว):**
- `contacts` ผู้ติดต่อ (ลูกค้า/ผู้ขาย + เลขภาษี/สาขา/ที่อยู่)
- `fin_docs` + `fin_doc_items` เอกสาร 4 ชนิด: quotation/invoice/receipt/expense · เลขรันต่อร้าน/ปี (QT-, INV-, RC-, EXP-, JV-) · VAT แยกนอก/รวมใน · หัก ณ ที่จ่าย · `share_key` ลิงก์สาธารณะ
- `fin_payments` รับ-จ่ายเงิน + สลิป + ผล EasySlip + กันสลิปซ้ำ (unique trans_ref)
- `expense_categories` 10 หมวดไทย seed ทุกร้าน + map รหัสบัญชี 5xxx
- **GL:** `chart_of_accounts` (ผังบัญชีไทย 23 บัญชี seed ทุกร้าน) + `journal_entries`/`journal_lines`
- `products.cost` (COGS) + `fin_doc_items.product_id` (ตัดสต๊อก)
- ปลด trigger กันสร้างร้านซ้ำ → เจ้าของ 1 คนมีได้ 20 กิจการ (สำนักงานบัญชี)

**เครื่องยนต์บัญชี (src/lib/finance-server.ts):** ทุกธุรกรรมลงเดบิต/เครดิตอัตโนมัติ บังคับ Dr=Cr · ขายเชื่อ→AR/รายได้/ภาษีขาย · ขายสด→เงิน+WHT asset · ค่าใช้จ่าย→หมวด5xxx/ภาษีซื้อ/AP หรือจ่ายสด · รับ-จ่ายชำระ · COGS/ตัดสต๊อก · ยกเลิก = reversal + คืนสต๊อก (audit-safe ไม่ลบ)

**หน้าจอ:** ภาพรวมการเงิน (cash flow, ค้างรับ-จ่าย, เกินกำหนด) · เอกสารขาย (ออก/แปลง QT→INV→RC/พิมพ์ A4+ตัวอักษรไทย/ลิงก์ลูกค้า) · ค่าใช้จ่าย (AI อ่านบิล → กรอกฟอร์ม + 50 ทวิ) · การเงิน/กระทบยอด (อัปสลิปจับคู่ + import statement CSV/Excel) · ผู้ติดต่อ · สินค้า/บริการ (ราคา+ต้นทุน+สต๊อก) · สมุดรายวัน (+JV เอง) · รายงาน+ภาษี (P&L, Aging AR/AP, ภ.พ.30 รายช่อง, ภ.ง.ด.3/53 + ไฟล์ .txt, งบทดลอง, export Excel) · ตั้งค่า/ทีม/สลับกิจการ

**ลิงก์เอกสารสาธารณะ `/doc/[share_key]`:** ลูกค้าเห็นเอกสาร + QR พร้อมเพย์ยอดค้าง + อัปสลิปเอง → ระบบตรวจ EasySlip + ตัดยอด + ลงบัญชีอัตโนมัติ (บันทึกเฉพาะสลิปที่ตรวจผ่านจริงและยอดไม่เกินค้าง)

**ผู้ช่วยบัญชี AI (`/dashboard/assistant`):** 18 tools ครอบทุกงาน — เรียก action ชุดเดียวกับ UI (saveDoc/recordPayment/convertDoc/voidDoc) จึงลง GL/สต๊อก/audit ครบเสมอ · แนบรูปบิลในแชท → `/api/finance/extract` (Mistral OCR→Gemini→Claude→GPT) → AI บันทึกให้ · โควตา 100 ข้อความ/วัน/ร้าน + extract 60 ไฟล์/วัน

## 🔴 สิ่งเดียวที่บล็อกการใช้งาน AI (เหมือนเดิมจากรอบก่อน)
**`ai_provider_keys` = 0 แถว** — เจ้าของต้องเข้า `/dashboard/admin` ใส่ API key อย่างน้อย 1 ค่าย (แนะนำ Gemini ฟรี aistudio.google.com หรือ Mistral สำหรับ OCR บิล) · ระบบเอกสาร/บัญชี/รายงาน **ใช้ได้เต็มโดยไม่ต้องมี key** (คีย์มือ) — key จำเป็นเฉพาะผู้ช่วย AI + อ่านบิล

## 🆕 รอบขัดเงา 2026-07-23 (รอบสอง — หลังเจ้าของทดสอบจริง)
- **แก้ OAuth เฟส**: `/?code=` หลุดมาหน้าแรก → middleware ส่งต่อไป `/auth/callback` + คนล็อกอินแล้วเข้าหน้าแรกเด้งเข้า dashboard เลย
- **รายงานดูได้ราย เดือน/ไตรมาส/ปี** (`?period=2026-07 | 2026-Q3 | 2026`) ทุกแท็บ + แถวรวมงวด
- **Global AI Quota ต่อเจ้าของ (migration 055/057)**: โควตางาน AI (assistant+ocr) นับรวมทุกกิจการของเจ้าของ เพดาน = แพ็กที่ดีที่สุดที่จ่าย — ปั๊มบริษัทฟรีไม่ได้โควตาเพิ่ม · RPC `consume_ai_quota` (advisory lock) + `get_ai_quota_status` (member check) · แจ้งเตือนอัตโนมัติ 80%/95% ลง notifications · `shops.ai_quota_override` ให้ admin ปรับรายบัญชี (ยังไม่มี UI — ตั้งผ่าน SQL/Claude ได้)
- **หลอดเครดิต AI บน sidebar** (เขียว→เหลือง→แดง) + **paywall สวยในแชท AI** เมื่อโควตาเต็ม (ไม่โชว์ error ดิบ) — คีย์เอกสารเองไม่โดนจำกัด
- **จ่ายแพ็กเกจตรง (migration 056)**: ปุ่ม "สมัคร — จ่าย 990" → QR ยอดเท่าราคาแพ็ก → สลิปผ่าน (อัตโนมัติ/แอดมินยืนยัน) → RPC `apply_plan_purchase` เปิดแพ็ก+ตัดค่าแพ็ก+ตั้งรอบบิล +1 เดือนทันที (idempotent, ต่อยอดเครื่อง run_plan_billing เดิม) · เติมเครดิตพับเป็นตัวเลือกเสริม
- **UX**: Empty state ทุกหน้ามี icon+ปุ่ม CTA · ปุ่มดาวน์โหลด template statement CSV · แชท AI ตอบพร้อม **ปุ่มลิงก์เอกสาร** (เปิด/พิมพ์/ลิงก์ลูกค้า) · แถวเอกสาร void ขีดฆ่าจาง + หน้า detail บอก "ยกเลิกโดยใคร เมื่อไหร่"

## 🆕 รอบสาม 2026-07-23 — Commercial-ready
- **ราคาใหม่ 4 แพ็ก (migration 058)**: Starter 990 (1 กิจการ, AI 150/ด, สลิป 300/ด) · Professional 1,990 ⭐ (3 กิจการ, AI 500, สลิป 1,000) · AI Executive 3,990 (5 กิจการ, AI 2,000, สลิปไม่จำกัด, ปลดล็อกไฟล์ยื่นภาษี) · Agency 9,900 (ไม่จำกัดกิจการ, AI 10,000) · free = ทดลองใช้ · **จุดขาย: พนักงานไม่จำกัดทุกแพ็ก** · migrate pro→professional, mini→starter, enterprise→agency แล้ว
- **โควตาสลิป/เดือน ต่อเจ้าของ** (RPC `check_slip_quota`) บังคับ 3 ทางเข้า (recordPayment, uploadAndMatchSlip, public slip) — เต็มแล้ว auto-verify ปิด แต่ยืนยัน manual ได้เสมอ
- **ลิมิตจำนวนกิจการตามแพ็ก** (RPC `can_create_company`) เช็คตอน createShop พร้อมข้อความชวนอัปเกรด
- **ไฟล์ยื่นสรรพากรมาตรฐาน RD Prep** (`src/lib/rd.ts`): pipe | เท่านั้น + rdClean ล้าง |/newline/tab + วันที่ DD/MM/YYYY **พ.ศ.** + เข้ารหัส **TIS-620** + CRLF · เพิ่ม .txt ให้รายงานภาษีซื้อ-ขายด้วย · **gate แพ็ก**: .txt ปลดล็อก executive/agency (platform admin ใช้ได้เสมอ) — ก่อนยื่นจริงรอบแรกให้ทดลอง import เข้าโปรแกรม RD Prep ยืนยันลำดับคอลัมน์
- **ศูนย์ AI (Admin)**: จัดกลุ่ม Accordion พับ/กาง + badge 🟢/🔴 ทุกหมวด + key แสดง masked ••••1234 + ตัด Embedding UI (ไม่ใช้หลัง pivot)
- **จัดการผู้ใช้ระบบ**: คอลัมน์ตั้งเพดานโควตา AI/วัน รายกิจการ (ai_quota_override) พร้อมปุ่มบันทึก
- **บีบอัดรูปก่อนอัปโหลดทุกจุด** (`src/lib/compress-image.ts` canvas ล้วน ไม่มี dependency ใหม่): สลิป/บิล/รูปสินค้า → JPEG ≤~300KB, ยาวสุด 1600px — ประหยัด Supabase Storage 1GB ได้หลักแสนใบ
- **กันบั๊ก padding มือถือ**: main pb ลดเหลือ 4.5rem+safe-area พอดี bottom nav (viewport-fit=cover อยู่แล้ว)
- **Vercel timeout**: เลือกใช้ maxDuration ต่อ route (60-120s, Vercel ปรับตามแพลนอัตโนมัติ) แทน Edge/streaming — โครง tool-loop 3 ค่าย + Supabase service ทำงานบน Node ได้เสถียรกว่า streaming refactor (streaming เป็น optimization เฟสถัดไป ถ้าเจอ timeout จริงบน Hobby ให้เปิด Fluid Compute ใน Vercel dashboard)
- หมายเหตุ: ระบบเชิญทีม (Unlimited Users) มีอยู่แล้วที่ ตั้งค่า → ทีมงาน (อีเมล + บทบาท, RLS แยกต่อกิจการ)

## 🆕 รอบสี่ 2026-07-24 — ศูนย์ AI แบบ Function-Centric (mig 059)
- **UI ใหม่เหลือ 2 การ์ด**: ① ผู้ช่วยบัญชี AI (สมองหลัก — ค่าย google/openai/anthropic) ② AI อ่านบิล OCR (mistral/google/openai/anthropic) — แต่ละการ์ด: dropdown ค่าย + **ช่องพิมพ์ชื่อโมเดลเองได้** (datalist แนะนำ) + key masked ••••1234 + badge 🟢/🔴 + ปุ่มทดสอบ (test-ai รองรับ `purpose`)
- **แก้โมเดลไม่ต้องวาง key ซ้ำ**: store_purpose_ai_key รับ p_key optional — ค่ายออกรุ่นใหม่ก็แก้ชื่อแล้วบันทึก จบ
- **Auto-Fallback อ่านบิล**: extract route ไล่ลำดับ การ์ด OCR → การ์ดผู้ช่วย → คีย์สำรอง (ai_provider_keys ทั้ง 8 ค่าย พับเก็บใน "ขั้นสูง") — ค่ายหลักล่มระบบสลับเองทันที
- **ตัดทิ้ง**: UI routing tiers (ขั้นที่ 2 เดิม ประหยัด/มาตรฐาน/พรีเมียม + embedding) + action saveRouting — ตาราง ai_settings ยังอยู่เป็น fallback data ของ resolveDefaultAiConfig เฉย ๆ
- **Seed แล้วบน production**: การ์ดผู้ช่วย = google/gemini-2.5-flash · การ์ด OCR = mistral/mistral-ocr-latest (คัดลอกจาก key เดิม ไม่ต้องกรอกใหม่) — **ทั้งสองการ์ดขึ้น 🟢 พร้อมใช้แล้ว**

## 🆕 รอบห้า 2026-07-24 — Guest AI Sandbox หน้าแรก (mig 060)
- **ทดลองคุยกับผู้ช่วยบัญชี AI ได้ฟรี 3 ครั้งบนหน้าแรก ไม่ต้องล็อกอิน** (`LandingSandboxChat` ใต้ตัวอย่างบทสนทนาเดิม) — PLG hook: หมดโควตาแล้วปุ่ม "สมัครใช้ฟรี" พาเข้า /login ทันที
- **ปลอดภัยโดยสถาปัตยกรรม ไม่ใช่แค่ prompt**: guest ไม่มี tool ใดๆ เลย (ไม่มี shop ให้ผูก จึงไม่มีข้อมูลจริงให้เข้าถึง/รั่วได้ตั้งแต่ต้น) ต่างจากข้อเสนอเดิมที่จะ "จำกัด tool call ไว้ 1 ครั้ง" — ไม่จำเป็นเพราะไม่มี tool ให้เรียกอยู่แล้ว
- **กันโกงโควตา 3 ชั้น**: (1) `sc_guest` คุกกี้ HttpOnly ตั้งจาก **middleware** ทุก request (แก้ไข/ปลอมจาก client ไม่ได้ ต่างจาก localStorage) จำกัด 3 ครั้งตลอดชีพต่อคุกกี้ (2) เพดาน 15 ครั้ง/IP/วัน (soft safety net กันสคริปต์ล้างคุกกี้วนสร้างใหม่จาก IP เดียว — ตั้งใจไม่ใช้ IP เป็นตัวจำกัดหลักเพราะ IP ร่วม (ออฟฟิศ/มือถือ) จะบล็อกคนดีจำนวนมาก) (3) เพดานรวม 300 ครั้ง/วันทั้งแพลตฟอร์ม กัน bot swarm
- **จำกัดความยาว 150 ตัวอักษร ฝั่ง server ด้วย** (ไม่ใช่แค่ frontend maxLength — ยิง API ตรงบายพาสไม่ได้)
- ใช้ Gemini Flash-Lite (ถูกสุด) ไม่ log เข้า `ai_usage_logs`/โควตาร้าน (แยกกระเป๋าคนละก้อนจากลูกค้าจริง) เพราะไม่มี shop_id ให้ผูก
- เช็ค `platform_billing_settings.ai_kill_switch` ก่อนตอบทุกครั้ง — **พบว่า kill switch ตัวนี้ปัจจุบันไม่ถูกบังคับใช้กับ AI ของร้านที่ล็อกอินแล้วเลย** (ตรวจสอบแล้ว: enforcement เดิมอยู่ใน edge functions ที่ถูกลบไปตอน pivot chatbot, เหลือแค่ tracking/UI ให้ admin ดู) — เป็นช่องโหว่แยกที่ควรแก้ต่างหาก ไม่ได้แก้ในรอบนี้ (ดูรายการงานถัดไป)

### Hardening รอบสอง (mig 061 — ทดสอบผ่านบน production แล้ว: 3 ครั้งตอบจริง ครั้งที่ 4 โดน HTTP 429)
- **Atomic RPC** `consume_guest_ai_quota(guest, ip_hash)`: advisory lock + นับ 3 ชั้น + insert ในทรานแซกชันเดียว (กัน race ยิงพร้อมกัน) — service_role เท่านั้น
- **ไม่เก็บ IP ดิบ (PDPA)**: HMAC-SHA256 (`RATE_LIMIT_IP_SECRET` ใน env ถ้าตั้ง, fallback service key) + IPv6 normalize เป็น /64 ก่อน hash
- **HTTP status ถูกต้อง**: 429+Retry-After เมื่อครบโควตา · 503 fail-closed เมื่อ RPC/kill switch ล่ม (ไม่ปล่อยผ่านเด็ดขาด) · 400 prompt ว่าง · 502+code `upstream_XXX` เมื่อค่าย AI ล่ม (วินิจฉัยได้โดยไม่รั่ว detail)
- **แก้บั๊ก AI ไม่ตอบ (เจอตอนทดสอบรอบแรก)**: เดิม hardcode gemini-2.5-flash-lite + maxOutputTokens 300 → Gemini 2.5 เป็น thinking model กินโทเคนจนตอบว่าง/ล้ม — แก้เป็น: ใช้คีย์จากการ์ดงานระบบ (purpose chat → assistant → คีย์สำรอง google) + ไล่โมเดลสำรองอัตโนมัติ + maxOutputTokens 1000

## 🆕 รอบหก 2026-07-24 — commit 283c69e (mig 062)
- **/signup แยกจาก /login**: ชื่อ+อีเมล+รหัสผ่าน 2 ช่อง (เช็คตรงกันสดๆ + ปุ่มตาโชว์รหัส) · สมัครผ่าน server action `auth.admin.createUser(email_confirm:true)` → **เข้าใช้ได้ทันที ไม่ต้องรอเมลยืนยัน** (แก้ปัญหา SMTP ฟรีส่งไม่ถึง hotmail = สมัครแล้วเงียบหาย) · CTA หน้าแรกทุกปุ่มชี้ /signup · **ทดสอบ e2e บน production แล้ว: สมัคร→ล็อกอินอัตโนมัติ→เข้าหน้าสร้างกิจการ ✓** (บัญชีทดสอบลบแล้ว)
- **Kill switch ครบทุก AI แล้ว** (ปิดช่องโหว่ข้อ 1 เดิม): `platform_ai_ok()` (mig 047 — เช็คทั้งสวิตช์ฉุกเฉิน+เพดาน $/วัน) ถูกเรียกใน assistantReply, /api/finance/extract และ guest sandbox (ตัวหลังอัปเกรดจากเช็คแค่ kill switch เป็นเช็คเพดานด้วย — ทดสอบบน production แล้วยังตอบปกติ ✓)
- **แก้บั๊กจริงที่เจ้าของเจอบนมือถือ** "ต้องมีรายการอย่างน้อย 1 บรรทัด" ตอน AI บันทึกบิล: saveDoc เคยกรองทิ้งรายการที่ไม่มี qty (ทั้งที่ schema บอก ไม่ระบุ=1) → แก้ filter + เพิ่ม `total_amount` fallback ใน create_expense (โมเดลไม่ส่ง items ก็ลงยอดรวมเป็นรายการเดียวได้)
- **UI มือถือ**: modal ทุกตัว (8 จุด) เปลี่ยนจาก bottom-sheet เป็นยึดด้านบน+scroll ได้ — คีย์บอร์ด iOS จะไม่ดันฟอร์มหลุดจอ (ต้นเหตุ "ปุ่มสร้างกิจการดำลอยค้างทุกหน้า" = modal ใน layout เปิดค้างข้ามหน้า + โดนคีย์บอร์ดดัน) · CompanySwitcher ปิด dropdown/modal อัตโนมัติเมื่อนำทาง · FAB ติชมเล็กลง, z ต่ำกว่าเมนูเพิ่มเติม, ซ่อนในหน้าผู้ช่วย AI, เพิ่ม padding ล่าง main กันปุ่มโดนบัง
- **Approval Flow (mig 062)**: พนักงาน (agent) บันทึกค่าใช้จ่าย → `approval_status=pending` ยังไม่ลง GL · owner/admin เห็นแบนเนอร์+ปุ่มในหน้าเอกสาร กดอนุมัติ = ตั้งหนี้ลงสมุดรายวันทันที / ปฏิเสธพร้อมเหตุผล · แท็บ "รออนุมัติ" ในหน้าค่าใช้จ่าย · เจ้าของบันทึกเอง = ผ่านทันทีเหมือนเดิม (ธุรกิจคนเดียวไม่สะดุด) · ยังไม่ได้ e2e กับบัญชี agent จริง
- **แจ้งเตือน LINE**: ใช้ **LINE Messaging API** (LINE Notify ปิดบริการ มี.ค. 2025 — สเปกที่ให้มาใช้ไม่ได้แล้ว) · ตั้งค่าที่หน้า ตั้งค่า → การ์ด 🔔 (Channel access token + User/Group ID + ปุ่มส่งทดสอบ) · token เก็บใน `shop_notify_settings` หลัง RLS ไม่มี policy = client อ่านไม่ได้ · ยิงแจ้งเมื่อมีค่าใช้จ่ายรออนุมัติ · helper `lib/line.ts` พร้อมต่อยอด (เช่น สรุป aging)

### UAT Approval Flow บน production (2026-07-24 — ผ่านแล้ว, commit 8666cab)
รันด้วยกิจการทดสอบแยก (สร้าง→ทดสอบ→ลบทิ้งหมดแล้ว): agent สั่งแชท AI "บันทึกค่าไฟ 2,340 จ่ายแล้ว" → ระบบบังคับเข้า pending ไม่ลง GL ไม่บันทึกจ่าย ✓ → owner เห็นแบนเนอร์กดอนุมัติ → JV-2026-0001 ลงถูก (Dr 5130 / Cr 2010 เจ้าหนี้) ✓
**บั๊กที่ UAT จับได้และแก้แล้ว:** Gemini เรียกแค่ tool อ่านหมวด แล้ว "อ้างว่าบันทึกแล้ว" โดยไม่เรียก create_expense เลย (เอกสารไม่เกิดจริง!) → แก้ 2 ชั้นใน engine.ts: เข้มกติกา prompt + ตาข่าย server-side (คำตอบอ้างสำเร็จแต่ไม่มี write-tool ถูกเรียก → บังคับวนอีกรอบให้ทำจริง) — ทดสอบซ้ำแล้วเรียก tool จริง ออก EXP-2026-0001 สำเร็จ

## 🆕 รอบเจ็ด 2026-07-25 — ความแม่นของ OCR + พิมพ์เอกสาร (commit c7516b5)
ปัญหาที่เจ้าของเจอ: บิลแยกยอดมั่ว, แนบรูปแล้วพิมพ์สั่งกำกับไม่ได้, print บนมือถือดูไม่ได้
- **หลักคิดที่เปลี่ยน:** เดิม "แนบ = บันทึกทันที" AI เลยต้องเดาเมื่อบิลกำกวม → เปลี่ยนเป็น **ไม่ชัดต้องถาม ห้ามเดา**
- `extract/route.ts`: prompt อ่าน `vat_mode` (inclusive/exclusive/none) + `discount` เพิ่ม, ห้ามใส่ 0 แทน "ไม่รู้", บังคับรายงาน `unclear[]`
- **`reconcile()` ฝั่ง server** (ไม่พึ่ง AI ตรวจตัวเอง): เช็ค Σ(qty×price)=subtotal และ subtotal−discount(±vat)=total คลาดได้ 1 บาท/1% · ไม่ลงตัว = ใส่ `issues[]` + `needs_confirm:true` — **จงใจไม่แก้ตัวเลขให้เอง** เพราะการแก้เงียบๆ = ซ่อน error ให้ไปโผล่ตอนยื่นภาษี
- assistant prompt: `needs_confirm` → ห้ามบันทึก ต้องทวนเลขแล้วถามรวบครั้งเดียว · **คำสั่งที่คนพิมพ์ชนะ OCR เสมอ**
- `chat.tsx`: แนบไฟล์แล้วค้างเป็น chip พิมพ์สั่งกำกับต่อได้ก่อนกดส่ง (เดิมแนบปุ๊บยิงปั๊บ สั่งอะไรไม่ได้เลย)
- `doc-form.tsx`: ใช้ `vat_mode` จริงจากบิล (เดิม hardcode inclusive ทุกใบที่มี VAT = ยอดเพี้ยนทั้งใบถ้าบิลบวก VAT ท้าย) + แถบเตือนสีเหลืองบอกจุดที่อ่านไม่ชัด
- `print/[id]` (commit 43e8faa): **บนจอ = กระดาษยืดตามความกว้างจอ** (มือถืออ่านได้เต็มตา ตัวหนังสือไม่ถูกย่อ) · **ตอนพิมพ์ = บังคับ A4 210×297mm ขอบ 15mm** ด้วย `@media print !important` · `table-fixed`+`colgroup`+`tabular-nums` เลขตรงหลักทุกบรรทัด · `@page margin:0` กันล้นหน้า 2 · `thead` ซ้ำเมื่อข้ามหน้า · หัวเอกสาร/สรุปยอด/ช่องเซ็น responsive · เพิ่มแถว "ยอดชำระจริง" หลังหัก ณ ที่จ่าย
  - ⚠️ บทเรียน: รอบแรกใช้ `transform: scale(calc(100vw / 210mm))` — **CSS หารความยาวด้วยความยาวไม่ได้** ทั้งกฎเป็นโมฆะเงียบๆ (ทดสอบบนจอ 375px เจอ `transform:none`) อย่าเชื่อว่า build ผ่าน = CSS ทำงาน
  - ทดสอบจริงบน production จอ 375px: ไม่มี horizontal scroll · ทุกแถวคอลัมน์ขวาตรงกันหมด `[64,188,228,280,331]` · ยอด 127,000 + VAT 8,890 = 135,890 − WHT 3,810 = 132,080 ถูกต้อง (กิจการทดสอบลบแล้ว)
- ทดสอบ `reconcile()` แยกเป็นสคริปต์ 7 เคส (บิลถูก/OCR อ่านราคาผิด/ไม่รู้โหมด VAT/อ่านยอดไม่ได้/ยอดเพี้ยน) ผ่านหมด — รวมเคสจริงจากภาพเจ้าของ (50×550=27,500 ลด 10 → 27,490) ที่ต้องไม่เตือนพร่ำเพรื่อ

## 🆕 รอบแปด 2026-07-25 — AI ถามด้วยปุ่ม + แนบหลายไฟล์ + กันซ้ำ (commit 2e0af32, mig 063)
**ขับเคลื่อนด้วยหลักฐานจริงจาก production ไม่ใช่การเดา:**
- feedback ผู้ใช้: *"ใช้งานยากตรงบันทึกก็เลือกได้แค่รูปเดียวเอง"* (จากหน้า /dashboard/expenses/…)
- ตรวจ DB เจอ EXP-2026-0002 กับ 0003 **ซ้ำกัน** ยอด 54.00 คู่ค้าเดียวกัน ห่างกัน 14 นาที · ทั้ง 3 ใบชื่อรายการเป็น `"อื่น ๆ — CJ 1446 Phuttha Bucha"` = AI อ่าน**สลิปโอนเงิน**แล้วเอาชื่อคนรับโอนมาเป็นชื่อค่าใช้จ่าย (อ่านย้อนหลังไม่รู้ว่าค่าอะไร)

สิ่งที่ทำ:
- **tool `ask_user`** — AI ถามพร้อมตัวเลือก 2-4 ข้อ หน้าบ้านเรนเดอร์เป็นปุ่มกด กดแล้วส่งคำตอบทันที (ลูกค้าใช้มือถือ พิมพ์ยาก) · เดินผ่าน `collectChoices()` ครบทั้ง 3 provider loop
- **บังคับถามแทนเดา** ใน system prompt: ไม่รู้ว่าบิลเป็นค่าใช้จ่ายจ่ายแล้ว/ตั้งหนี้/พนักงานเบิกคืน/ขายให้ลูกค้า → ต้องถาม · `doc_kind:"slip"` → ต้องถามว่าโอนค่าอะไร ห้ามลง "อื่น ๆ" · needs_confirm/duplicate → ห้ามบันทึกเอง
- **กันบันทึกซ้ำ**: create_expense เช็คยอดเท่ากัน + คู่ค้าเดิม ภายใน 3 วัน → คืน `duplicate_suspected` ให้ไปถามก่อน (ปลดล็อกด้วย `confirm_duplicate:true` เมื่อเจ้าของยืนยัน)
- **ปุ่มทำต่อ** หลังออกเอกสาร (รับเงินเลย / แปลงเป็นใบแจ้งหนี้ / แนบบิลใบต่อไป) ผ่าน `next_choices`
- **แนบหลายไฟล์**: แชท AI เลือกได้ทีละ 8 ใบ อ่านทีละใบพร้อมตัวนับ · ฟอร์มค่าใช้จ่ายแนบหลายใบต่อ 1 เอกสาร (**mig 063 `fin_doc_files`** — ย้ายไฟล์เดิมเข้าตารางใหม่อัตโนมัติ, คง `file_path` เป็นไฟล์หลักไม่ให้ของเก่าพัง) · หน้าเอกสารแสดงครบทุกไฟล์
- **ปรับแพ็กทุกกิจการที่มีอยู่ตอนนี้เป็น Professional** (5 กิจการ: admin, King, Yedheeashop, ยังเติม, ทีเด็ดอาหารปลา) เพื่อทดสอบจริง — คนสมัครใหม่ยังได้ free ตามเดิม

**ผลทดสอบบน production (กิจการทดสอบ สร้าง→ทดสอบ→ลบแล้ว):** ปุ่มตัวเลือกเรนเดอร์จริงหลังบันทึกค่าใช้จ่าย ("แนบบิลใบต่อไป" / "ดูยอดค่าใช้จ่ายเดือนนี้") · กดปุ่มแล้วส่งคำสั่งต่อได้จริง AI ตอบยอดถูก ✓
**ยังไม่ได้พิสูจน์กับโมเดลจริง:** กฎ prompt ใหม่ 2 ข้อ (ถามก่อนเมื่อไม่รู้ว่าจ่ายแล้วหรือยัง / ห้ามตั้งชื่อรายการเป็นชื่อร้าน) — commit 838411a, 3d14954 · deploy แล้วแต่ทดสอบซ้ำไม่ได้เพราะ headless browser ค้างที่ streaming shell · **ให้เจ้าของลองพิมพ์ "มีบิลค่าน้ำมัน 800 บาท" (ไม่บอกว่าจ่ายยัง) แล้วดูว่าขึ้นปุ่มถามไหม**
> บทเรียน: กฎที่อยู่ใน prompt อย่างเดียว = โมเดลอาจไม่ทำตาม · เรื่องที่ผิดแล้วเสียหาย (ยอดไม่ลงตัว/บันทึกซ้ำ/ชื่อรายการว่าง) ต้องมีตาข่ายฝั่ง server ด้วยเสมอ — ในระบบนี้มีครบทั้ง reconcile(), duplicate check, fallback ชื่อรายการ

## 🆕 รอบเก้า 2026-07-25 — แชท AI ใช้งานจริงลื่นขึ้น (commit efbfd4c)
บั๊กที่เจ้าของถ่ายภาพส่งมา 3 ข้อ แก้ครบ:
- **แชทหายเมื่อสลับหน้า** → เก็บใน `sessionStorage` แยกตามกิจการ (`sc_chat_<shopId>`) เก็บ **20 ข้อความล่าสุด** เกินแล้วตัดเก่าสุดออกทีละอัน · มีปุ่ม "ล้างแชท" · ไม่เก็บ blob url ของรูป (ตายเมื่อรีเฟรช) เก็บชื่อไฟล์แทน
- **กดส่งเหมือนไม่ติด** → เดิมรอ OCR ครบทุกใบก่อนถึงขึ้นบับเบิล (4 ใบ = เงียบ ~40 วิ) → เปลี่ยนเป็น **ขึ้นบับเบิล+รูปย่อทันทีที่กดส่ง** แล้วอ่านบิลเบื้องหลัง พร้อมตัวนับ "กำลังอ่านบิลใบที่ 3/4" และปุ่มส่งหมุนเป็น spinner
- **ส่งแล้วไม่เห็นรูป** → โชว์รูปจริงในบับเบิล + preview ก่อนกดส่ง (ลบทีละใบได้)
- **หลายไฟล์: ถามรวบครั้งเดียว** ("บิล 4 ใบนี้บันทึกเป็นอะไรทั้งหมด") + ตัวเลือก "คละกัน แยกทีละใบ" — ห้ามไล่ถามทีละใบตั้งแต่แรก
- **บังคับ paid_now ที่ server**: `create_expense` ไม่ส่ง `paid_now` → ตีกลับ `need_paid_status` ให้ไปถามก่อน (ไม่พึ่ง prompt อีกต่อไป — ครั้งก่อน Gemini เดาว่าจ่ายแล้วทั้งที่ผู้ใช้ไม่ได้บอก)

## 🆕 รอบสิบ 2026-07-25 — เชื่อม LINE คลิกเดียว (commit eba884d, mig 064)
**ปัญหา:** วิธีเดิมต้องให้ลูกค้าไป developers.line.biz สร้าง OA เอง + คัดลอก Channel access token — SME ส่วนใหญ่ทำไม่ได้ = ฟีเจอร์ตายทั้งที่โฆษณาไว้
**วิธีใหม่ (LINE Login + OA กลาง):** แพลตฟอร์มมี OA ตัวเดียว ลูกค้ากดปุ่มเดียว → ล็อกอิน LINE → ได้ userId → push ได้ทันที
- ต้องสร้าง **Provider เดียว** ที่มีทั้ง Messaging API channel และ LINE Login channel (userId ถึงจะตรงกัน) · Callback URL = `https://sudochatbot.online/api/line/callback` · ผูก OA เข้ากับ LINE Login channel (Linked OA)
- `bot_prompt=aggressive` ชวนเพิ่มเพื่อน OA ในสเต็ปเดียวกัน — ไม่เพิ่มเพื่อน = push 403 ตลอด จึงต้องรวบให้จบ
- callback ยิงข้อความต้อนรับทันที = พิสูจน์ว่าใช้ได้จริงโดยไม่ต้องให้ผู้ใช้กดทดสอบเอง · 403 → บอกตรงๆ ว่า "ยังไม่ได้เพิ่มเพื่อน"
- state ลงลายเซ็น HMAC (`lib/line-state.ts`) กัน CSRF/สลับร้าน · **หมายเหตุ**: route file ของ Next.js export ฟังก์ชันอื่นนอกจาก HTTP method ไม่ได้ (build fail) จึงต้องแยกไปไว้ lib
- คงทางเลือก **OA ของร้านเอง** ไว้ใน "ตั้งค่าขั้นสูง" (สำหรับคนอยากได้แบรนด์ตัวเอง/ส่งเข้ากลุ่มทีม) — `link_source` เป็นตัวแยกว่าใช้แบบไหน
- **ยังไม่ทำงานจนกว่าเจ้าของจะตั้งค่า**: หน้าแอดมิน → การ์ด "บัญชี LINE กลางของแพลตฟอร์ม" ใส่ 4 ค่า (Login Channel ID/Secret, OA token, @basic id) · ก่อนตั้ง หน้าตั้งค่าของร้านจะแจ้งว่ายังไม่พร้อมและให้ใช้ขั้นสูงไปก่อน
- ⚠️ ค่าใช้จ่าย: LINE OA ฟรี 300 ข้อความ/เดือน — ถ้าลูกค้าเยอะต้องอัปแพ็ก LINE (Basic ~฿1,200/เดือน 15,000 ข้อความ) เพราะข้อความทุกร้านนับรวมที่ OA กลาง · ถ้าไม่อยากรับต้นทุนนี้ ให้ผลักลูกค้าไปใช้ "OA ของร้านเอง" แทน

## 🆕 รอบสิบเอ็ด 2026-07-25 — LINE ใช้งานได้จริง + ยกระดับดีไซน์ + ปิดช่องโหว่ (mig 064-067)
**LINE (ทดสอบส่งข้อความจริงสำเร็จ HTTP 200 · ภาษาไทยถูกต้อง):**
- ตั้งค่า OA กลางเสร็จ: Login channel 2010846812 · OA `@097hgbvt` (SudoChatbot) · โควตา 300 ข้อความ/เดือน
- 🐛 **บั๊ก 400 Invalid redirect_uri**: เดิมสร้าง redirect_uri จาก `request.origin` → เปิดจาก `*.vercel.app` แล้วไม่ตรงกับ Callback ที่ลงทะเบียนไว้ → แก้เป็นบังคับโดเมนหลักเสมอ (`lib/app-origin.ts`)
- **Rich Menu**: สร้างจากหน้าแอดมินปุ่มเดียว ระบบวาดรูปเอง (ImageResponse + Noto Sans Thai 2 น้ำหนัก) · **เลิกใช้อีโมจิ** เปลี่ยนเป็นไอคอนเส้น SVG สีแบรนด์ในวงกลมนุ่ม — อีโมจิทำให้ดูเหมือนงานสไลด์ ไม่ใช่ผลิตภัณฑ์
- ⚠️ Login channel ต้องกด **Published** (จาก Developing) ไม่งั้นมีแต่เจ้าของล็อกอินได้
- ⚠️ token/secret ที่เคยวางในแชทควร rotate แล้วอัปเดตผ่านหน้าแอดมิน

**ดีไซน์ (components/ui.tsx — แก้ที่เดียวสวยทั้งแอป):** Card เงาสองชั้น · Badge มี ring · EmptyState ใหญ่ขึ้น · ปุ่มเพิ่ม `brand`/`lg` · focus-visible ring ทุกที่ (a11y) · input สูง 44px บนมือถือ · เพิ่ม `Field` (label+hint+error) และ `StatCard`

**หน้าแรก:** ปุ่มรอง "ดูราคา" · แถบความมั่นใจใต้ CTA · **ตารางเทียบ "แบบเดิม vs กับเรา"** 4 ข้อจากจุดเจ็บจริง · แถบ CTA ติดล่างบนมือถือ

**ความปลอดภัย (จาก Supabase advisor — ของจริงไม่ใช่เดา):**
- 🔴 **ช่องโหว่จริงที่ปิดแล้ว (mig 066)**: `seed_chart_of_accounts(p_shop_id)` / `seed_expense_categories(p_shop_id)` เป็น SECURITY DEFINER ไม่เช็คสิทธิ์ และ **anon เรียกผ่าน REST ได้** → ยัดผังบัญชี/หมวดค่าใช้จ่ายใส่ร้านคนอื่นได้ · ถอน execute แล้ว trigger ยังทำงานปกติ (รันด้วยสิทธิ์เจ้าของ) · ปิด trigger function อื่นที่โผล่เป็น RPC ด้วย + ล็อก search_path `normalize_email`
- ✅ ตรวจแล้วว่า `claim_platform_admin()` ปลอดภัย (คืน false ทันทีถ้ามีแอดมินอยู่แล้ว)
- **mig 067**: ตัวนับ rate limit กลาง `consume_public_rate` (HMAC IP ไม่เก็บ IP ดิบ + advisory lock) · ใช้กับสมัครสมาชิก 10 ครั้ง/ชม./IP fail-closed
- ⏳ **ยังต้องเจ้าของกดเอง**: เปิด *Leaked password protection* ใน Supabase Dashboard → Authentication → Policies

## 🆕 รอบสิบสอง — LINE webhook: ข้อความทางการคุมจากโค้ด (mig 068, commit c0ab286)
`/api/line/webhook` — เลิกพิมพ์ข้อความใน OA Manager แล้วลืมว่าเขียนอะไรไว้ ทุกข้อความอยู่ใน git
- **ตรวจลายเซ็น `x-line-signature` ทุกครั้ง** ด้วย `timingSafeEqual` (กัน timing attack) — ทดสอบแล้ว: ไม่มีลายเซ็น/ลายเซ็นปลอม → **401** · ลายเซ็นถูก → **200** ✓
- `follow` → ข้อความต้อนรับ + วิธีเริ่มใช้ 3 ขั้น · `message` → ตอบแบบมีประโยชน์ แยกกรณีเชื่อมบัญชีแล้ว/ยังไม่เชื่อม + คีย์เวิร์ด "เมนู" · `unfollow` → ตัดการเชื่อมของกิจการนั้นอัตโนมัติ (ไม่ยิงข้อความทิ้งเปล่า)
- error หรือยังไม่ตั้งค่า = ตอบ 200 เสมอ กัน LINE retry ถล่ม
- ⚠️ **ต้องตั้งใน OA Manager**: ใส่ Webhook URL `https://sudochatbot.online/api/line/webhook` + เปิด "ใช้ webhook" + **ปิดตอบกลับอัตโนมัติ/ข้อความต้อนรับของ LINE** (ไม่งั้นข้อความซ้อนกัน) + เปลี่ยน Response mode เป็น **Bot**

## 🔲 งานถัดไป (เรียงตามผลกระทบ)
0. **ข้อมูลจริงที่ต้องจัดการ**: EXP-2026-0003 (King) เป็นเอกสารซ้ำของ EXP-2026-0002 — ให้เจ้าของกด "ยกเลิก" ในหน้าเอกสาร (ระบบกลับรายการ GL ให้เอง) **ห้ามลบด้วย SQL** เพราะจะทิ้ง journal entry ค้าง
2. **ทดสอบ end-to-end บน production**: ออก INV → ลิงก์ลูกค้า → สลิป → รายงานภาษี (สมัครสมาชิก + approval flow + GL ผ่านแล้ว)
4. **แจ้งเตือน due date** เข้าอีเมล/LINE (โครง LINE พร้อมแล้วใน lib/line.ts — เหลือ cron รายวันสรุป aging)
5. ไฟล์ .txt ภ.ง.ด./ภ.พ.30 เป็น pipe-format + TIS-620 แล้ว (mig 059) — ก่อนยื่นจริงรอบแรกให้ทดลอง import เข้าโปรแกรม RD Prep ยืนยันลำดับคอลัมน์ตรงเวอร์ชันล่าสุด
6. ลบ edge functions ค้างบน Supabase dashboard + พิจารณา drop ตารางแชทบอทเก่าเมื่อแน่ใจ
7. Facebook App "SudoLogin" ยังใช้กับ OAuth login ได้ตามเดิม — ส่วน Meta review สำหรับ chatbot ไม่ต้องทำแล้ว

### ⚠️ ความปลอดภัย (ค้างจากรอบก่อน — ยังแนะนำ rotate)
Vercel token · Supabase management token · Facebook App Secret · รหัสบัญชีทดสอบ

## 🗂️ RPC ใหม่ที่สำคัญ
`next_fin_doc_number(shop, type)` เลขรันเอกสาร/JV · `seed_expense_categories` / `seed_chart_of_accounts` (trigger ตอนสร้างร้าน) · เดิมที่ยังใช้: `credit_wallet`, `billing_summary`, `run_plan_billing`, `decrement_stock` (qty ติดลบ=คืน), `is_platform_admin`, `store/get_ai_key`, `get_purpose_ai_key`, `store/get_shop_slip_key`, `admin_confirm_topup`, `next_invoice_number` (ใบเสร็จแพลตฟอร์ม)

## 🧪 ทดสอบ SQL
สร้าง auth.users ต้องใส่ token columns เช่น `confirmation_token` เป็น `''` ไม่ใช่ NULL (เคยทำ auth พังทั้งระบบมาแล้ว)

## 🆕 รอบสิบสาม 2026-07-26 — Auth แยกชัด · เอกสารกฎหมายตรงจริง · แก้ Cold Start (mig 069)
**Auth (commit f7e43d7, da783b7):** Google อยู่ทั้ง 2 หน้า (เดิม /signup ไม่มีเลย ทั้งที่ Google = 5 จาก 10 บัญชี)
· Facebook คงไว้ตามที่เจ้าของตัดสินใจ แสดงเท่ากันทั้ง 2 หน้า (เดิมโผล่หน้าเดียว = ต้นเหตุกดในหน้า login แล้วได้บัญชีใหม่)
· คำบนปุ่มเปลี่ยนตามบริบท "สมัครด้วย"/"เข้าสู่ระบบด้วย" — OAuth ของ Supabase ไม่มีโหมดห้ามสมัคร แก้ได้แค่ที่ความชัดเจน
· **LINE Login: ตัดสินใจไม่ทำ** — Supabase ไม่รองรับ LINE ต้องเขียน session bridge เอง = เสี่ยง session hijacking · ใช้ผูก LINE เพื่อแจ้งเตือนแทน

> ⚠️ **กับดักที่เจอตอนจะลบ Facebook**: `ta_free14@hotmail.com` ล็อกอินด้วย Facebook อย่างเดียว **และเป็น platform admin คนเดียวของระบบ** — ถ้าลบ provider จะเข้าศูนย์ AI ไม่ได้ตลอดกาล · แก้แล้วโดยเพิ่ม `justin.minnie89@gmail.com` (Google) เป็นแอดมินคนที่ 2

**เอกสารกฎหมาย:** privacy/terms ยังเขียนว่าเป็น "แชทบอท AI สำหรับร้านค้าบน Facebook/IG" ทั้งที่ pivot ไปแล้ว — และเป็น URL ที่ลงทะเบียนกับ LINE · เขียนใหม่ให้ตรงระบบจริง + เพิ่มหัวข้อการใช้ AI, การเชื่อม LINE, และระบุชัดว่าเงินเข้าบัญชีลูกค้าโดยตรง

**Cold Start (mig 069):** ผู้ใช้ใหม่เจอเลข 0 สี่ช่อง + เช็คลิสต์ที่เป็นงานตั้งค่าล้วน = ขอให้ตั้งค่าก่อนให้คุณค่า
· **Sample Data 1 คลิก** (`is_sample` flag) สร้าง 4 เอกสารครบวงจร ล้างทิ้งได้สะอาดรวม journal entries
· **Quick-create FAB** ปุ่ม + ลอย 4 ทางลัดจากทุกหน้า

**ลบโค้ดตาย:** `lib/channel-errors.ts`, `lib/ads-errors.ts` (Meta API ยุคแชทบอท) · ตรวจแล้วทุก npm dependency ยังมีที่ใช้จริง

## 🆕 รอบสิบสี่ 2026-07-26 — แดชบอร์ดที่ "บอกว่าต้องทำอะไร" (commit adcf3e6)
เดิม: หัวข้อ "ภาพรวมการเงิน" + เลข 4 ช่องลอยๆ ผู้ใช้ต้องคิดเองว่าทำอะไรต่อ
- **ทักทายตามเวลาไทย + วันที่ + สรุปหนึ่งบรรทัด** ("เดือนนี้เงินสดเป็นบวก X · รอเก็บอีก Y")
- **แผง "วันนี้ต้องทำ"** (`today-panel.tsx`) — ระบบคิดงานให้ เรียงตามเร่งด่วน กดแล้วไปทำได้ทันที:
  ตามเงินเกินกำหนด · บิลต้องจ่าย · ค่าใช้จ่ายรออนุมัติ · สลิปรอจับคู่ · เตือน ภ.พ.30 ล่วงหน้า 7 วัน
  ไม่มีงานค้าง → "เคลียร์หมดแล้ว 🎉" (inbox-zero)
- **ตัวเลขเทียบเดือนก่อน** — สีเขียวตามบริบท (เงินเข้าขึ้น=ดี / เงินออกขึ้น=ไม่ดี) ตัวเลขเดี่ยวไม่มีความหมายถ้าไม่มีจุดอ้างอิง
- การ์ดยอดค้างมีลิงก์ไปหน้าที่เกี่ยวข้องในตัว · ใช้ `StatCard` กลาง

**หลักที่ยึดตลอด 2 รอบนี้:** อย่าโชว์ข้อมูลแล้วให้ผู้ใช้ตีความเอง — บอกไปเลยว่าต้องทำอะไรและกดตรงไหน

## 🆕 รอบสิบห้า 2026-07-26 — ทุกหน้าบอกว่า "ใช้ทำอะไร" (commit 95a8f33)
ปัญหา: คนไทยที่ไม่เคยใช้โปรแกรมบัญชีเปิดแต่ละหน้าแล้วไม่รู้ต้องทำอะไร — คำอธิบายเดิมใช้ศัพท์บัญชี ("ตั้งหนี้", "กระทบยอด", "COGS")
- `PageHeader` กลางใน `components/ui.tsx` = ชื่อหน้า + ตัวเลขสำคัญ + กล่อง 💡 อธิบายภาษาชาวบ้าน + ปุ่มหลัก (มือถือเต็มความกว้าง)
- ใช้ครบ 10 หน้า · เปลี่ยนคำเป็นภาษาคน: "ยอดค้างรับ" → "ลูกค้ายังค้างเราอยู่" · สมุดรายวันบอกตรงๆ ว่า "หน้านี้ไม่ต้องทำอะไรเลย"
- คงธีม minimal — ไม่เพิ่มสี ไม่เพิ่มกรอบ เพิ่มแค่บรรทัดที่ทำให้เข้าใจ

**หลักการที่ใช้ตัดสินใจทุกครั้ง:** ถ้าผู้ใช้ต้องเดาว่าหน้านี้ทำอะไร = เราออกแบบพลาด ไม่ใช่ผู้ใช้โง่

---

## 🆕 รอบสิบหก 2026-07-26/27 — ปิดข้อ 🔴 ของผู้สอบบัญชีครบ (18 commit: 1ee05a7 → 589b6ad)

รอบนี้ยาวและแตะแกนบัญชี ถ้ามารับงานต่อ **อ่านหัวข้อนี้ก่อนแตะโค้ดการเงิน**

### กติกาที่ห้ามละเมิด (เกิดจากบั๊กจริงในรอบนี้)

1. **กฎภาษี/บัญชีเขียนได้ที่เดียว** — `src/lib/tax-th.ts` (กฎหมาย) · `src/lib/finance.ts` (`calcDocTotals`) · `src/lib/vat-docs.ts` (ใบไหนนับเป็นภาษีขาย/ซื้อ) · `src/lib/depreciation.ts`
   บั๊กร้ายแรงทุกตัวในรอบนี้เกิดจากกฎเดียวกันถูกเขียนสองที่แล้วเพี้ยนจากกัน
2. **รายงานต้องตรงกับสมุดรายวันเสมอ** — วิธีพิสูจน์: เขียน SQL เทียบ "สิ่งที่รายงานจะแสดง" กับยอดในบัญชี 2030 แยกรายเดือนรายกิจการ ผลต่างต้องเป็น 0.00 ทุกแถว
3. **เพิ่มค่าใหม่ให้ enum ต้องไล่ทุกที่ที่ hardcode รายการค่านั้น** — พลาดมาแล้ว 2 รอบในวันเดียว (ใบลดหนี้หายจากหน้ารายการ และหายจากลิงก์สาธารณะ)
4. **เพิ่มบัญชีใหม่ต้องตรวจว่ารหัสว่างจริง แล้วตรวจ "ชื่อ" หลังรัน** — เคยใช้ 4090 ซึ่งชนกับ "รายได้อื่น" ที่มีอยู่แล้ว คำสั่งมี `where not exists` จึงไม่ทำอะไรเงียบ ๆ และตอนตรวจนับแค่จำนวนแถวเลยไม่เห็น
5. **ยืนยัน deploy ด้วย `curl /api/health` เทียบ commit กับ git** ห้ามใช้ HTTP status
6. **รันชุดตรวจก่อน commit เสมอ: `npm run verify`**

### สิ่งที่เพิ่มเข้ามา

| เรื่อง | ไฟล์/ตารางหลัก | ฐานกฎหมาย |
|---|---|---|
| จุดความรับผิด VAT แยกสินค้า/บริการ | `fin_docs.tax_point` · ตาราง `vat_recognitions` · `applyPaymentToDoc` | ม.78 / ม.78-1 |
| ใบลดหนี้ / ใบเพิ่มหนี้ | `issueCreditDebitNote` · `note-dialog.tsx` · บัญชี **4190** | ม.86/10 · ม.86/9 |
| ล็อกงวดที่ยื่นภาษีแล้ว | ตาราง `fin_period_locks` + **trigger `assert_period_open`** | — |
| ทรัพย์สิน + ค่าเสื่อม + ปิดบัญชีสิ้นปี | `dashboard/assets/*` · `lib/depreciation.ts` · `fixed_assets` `depreciation_runs` `fiscal_closes` | ม.65 ทวิ (2) · พ.ร.ฎ.145 |
| อัตรา VAT ตามช่วงเวลา | ตาราง `vat_rates` · `vat_rate_on()` · `vat_rate_status()` | พ.ร.ฎ.ลดอัตรา |
| ประเภทผู้รับเงิน ภ.ง.ด.3/53 | `contacts.recipient_kind` + snapshot ที่ `fin_docs` | — |

### จุดที่ต้องระวังเป็นพิเศษ

- **ล็อกงวดอนุญาตให้แก้ได้เฉพาะ** `paid_amount / status / notes / share_key / file_path / approval_*`
  เวอร์ชันแรกบล็อกทุกอย่างซึ่งทำให้ **เก็บเงินลูกหนี้ใบเก่าไม่ได้** = ใช้งานจริงไม่ได้ ถ้าจะเพิ่มคอลัมน์ใหม่ให้คิดด้วยว่าควรอยู่ใน allow-list ไหม
- **ใบลดหนี้ต้องใช้อัตรา VAT ของใบเดิม** ไม่ใช่อัตราวันนี้ — สำคัญมากถ้าอัตราเปลี่ยนหลัง 30 ก.ย. 2569
- **ค่าเสื่อมงวดสุดท้ายบังคับปิดให้พอดี** กันเศษค้างจากการปัดเศษรายเดือน
- **ยอดในฐานข้อมูลเก็บเป็นบวกเสมอ** แม้แต่ใบลดหนี้ — ใส่เครื่องหมายตอนรวมยอดผ่าน `vatSign()` ที่เดียว

### ยังไม่ได้ทำ

- **e-Tax Invoice** — ดู `docs/E-TAX-INVOICE-PLAN.md` ติดที่ PDF/A-3 ที่ยืนยัน conformance จากเครื่องพัฒนาไม่ได้ ต้องใช้ veraPDF
- **ทดสอบไฟล์ RD Prep กับโปรแกรมจริง** — รูปแบบผ่านชุดตรวจแล้ว แต่ยังไม่เคยนำเข้าโปรแกรมจริง
- **8 หน้าจอที่ยังไม่มีใครกดจริง** — ผู้ติดต่อ · ชุดส่งสำนักงานบัญชี · พิมพ์ใบกำกับภาษี · ปิดงวด · ใบลดหนี้ · สินค้า/บริการบนใบแจ้งหนี้ · ทรัพย์สิน · หน้าแรก

### เจ้าของต้องทำเอง

เปิด Leaked Password Protection ใน Supabase · rotate LINE token 3 ตัว (วางในหน้าแอดมินเท่านั้น) · **ติดตามประกาศต่ออายุ VAT ก่อน 30 ก.ย. 2569** (ระบบเตือนเองเมื่อเหลือ ≤45 วัน)
